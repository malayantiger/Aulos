<Controls:MetroWindow x:Class="Aulos.UI.Views.ShellView"
                      xmlns:views="clr-namespace:Aulos.UI.Views"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      xmlns:cal="http://www.caliburnproject.org"
                      xmlns:p="clr-namespace:Aulos.UI.Properties"
                      xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                      Title="{Binding Source={x:Static p:Resources.ShellViewWindowTitle}}" Width="640" Height="480">
    <StackPanel>
        <DockPanel LastChildFill="False">
            <ContentControl x:Name="CommandBar"
                            DockPanel.Dock="Top" />
        </DockPanel>
        <ContentControl x:Name="ActiveItem" />
    </StackPanel>
</Controls:MetroWindow>
